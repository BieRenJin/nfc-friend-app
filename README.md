# NFC Friend App

一个基于 React + Vite 的移动端应用，支持签到和交友功能。

## 功能特性

### 🔐 双模式切换
- **签到模式**：当用户确认登录后，显示签到欢迎页面
- **交友模式**：未登录时，显示添加好友的界面

### 📱 页面说明

1. **主页面 (`/`)**
   - 根据 localStorage 中的 `login` 状态显示不同页面
   - 支持 URL 参数 `?name=xxx` 动态设置用户名

2. **设置登录页面 (`/set-login`)**
   - 点击"确定"按钮设置登录状态
   - 支持 URL 参数 `?name=xxx` 保存用户名到 localStorage

3. **签到页面**（CheckInPage）
   - 显示欢迎文字和用户名
   - 显示用户头像
   - 绿色打勾图标动画

4. **交友页面**（FriendPage）- 两个状态
   - 状态1：询问是否添加好友
   - 状态2：添加成功，显示两个头像

### ✨ 动画效果

- 页面切换的淡入淡出
- 按钮点击的缩放效果
- 成功状态的打勾动画
- 头像和文字的平滑过渡

## 技术栈

- **React 19** - UI 框架
- **Vite** - 构建工具
- **React Router** - 路由管理
- **Framer Motion** - 动画库
- **Tailwind CSS** - 样式框架

## 本地开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173`

### 构建生产版本

```bash
npm run build
```

## 部署到 Vercel

### 方法 1: 使用 Vercel CLI

1. 安装 Vercel CLI：
```bash
npm install -g vercel
```

2. 在项目根目录运行：
```bash
vercel
```

3. 按照提示完成部署

### 方法 2: 通过 Vercel 网站

1. 访问 [vercel.com](https://vercel.com)
2. 点击 "Import Project"
3. 选择你的 Git 仓库
4. Vercel 会自动检测到 Vite 项目并配置
5. 点击 "Deploy"

### 方法 3: 从本地部署

1. 构建项目：
```bash
npm run build
```

2. 上传 `dist` 文件夹到 Vercel

## 使用说明

### URL 参数

应用支持通过 URL 参数设置用户名：

```
https://your-app.vercel.app/?name=Caroline
https://your-app.vercel.app/set-login?name=Caroline
```

### localStorage 数据结构

```javascript
{
  "login": "true",        // 是否已登录（签到）
  "userName": "Caroline"  // 用户名
}
```

### 使用流程

1. **初次访问**：
   - 访问 `https://your-app.vercel.app/?name=YourName`
   - 显示交友页面（未登录状态）

2. **设置登录**：
   - 访问 `https://your-app.vercel.app/set-login?name=YourName`
   - 点击"确定"按钮
   - localStorage 会保存 `login: true` 和 `userName: YourName`

3. **签到模式**：
   - 再次访问 `https://your-app.vercel.app`
   - 自动显示签到页面

### 清除登录状态

在浏览器控制台运行：
```javascript
localStorage.removeItem('login');
```

## 自定义头像

将你的头像图片放在 `public/images/` 文件夹中：

- `avatar-default.png` - 对方用户的头像
- `avatar-current.png` - 当前用户的头像

支持的格式：PNG、JPG、SVG

## 项目结构

```
nfc-friend-app/
├── public/
│   └── images/              # 头像图片
│       ├── avatar-default.svg
│       ├── avatar-current.svg
│       └── check-icon.svg
├── src/
│   ├── components/          # 可重用组件
│   │   ├── StatusBar.jsx    # iOS 状态栏
│   │   ├── BottomNav.jsx    # 底部导航栏
│   │   └── CheckIcon.jsx    # 打勾图标
│   ├── pages/               # 页面组件
│   │   ├── MainPage.jsx     # 主页面（条件渲染）
│   │   ├── SetLoginPage.jsx # 设置登录页面
│   │   ├── CheckInPage.jsx  # 签到页面
│   │   └── FriendPage.jsx   # 交友页面
│   ├── App.jsx              # 应用入口和路由
│   └── index.css            # 全局样式
├── vercel.json              # Vercel 配置
└── package.json             # 项目依赖
```

## iOS Safari 兼容性

此应用完全支持 iOS Safari，包括：
- localStorage 数据持久化
- 触摸交互
- 平滑动画
- 响应式设计

## 许可证

MIT
