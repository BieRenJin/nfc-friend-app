# 快速开始指南

## 🚀 本地运行

1. **安装依赖**
```bash
npm install
```

2. **启动开发服务器**
```bash
npm run dev
```

3. **访问应用**
打开浏览器访问：`http://localhost:5173`

## 📝 使用示例

### 测试交友模式（未登录）
```
http://localhost:5173/?name=Caroline
```

### 测试签到模式（已登录）
1. 先访问：`http://localhost:5173/set-login?name=Caroline`
2. 点击"确定"按钮
3. 再访问：`http://localhost:5173`

### 切换回交友模式
在浏览器控制台运行：
```javascript
localStorage.removeItem('login');
```
然后刷新页面

## 🖼️ 自定义头像

替换 `public/images/` 文件夹中的图片：
- `avatar-default.svg` → 替换为 `avatar-default.png` 或 `avatar-default.jpg`
- `avatar-current.svg` → 替换为 `avatar-current.png` 或 `avatar-current.jpg`

## 🌐 部署到 Vercel

### 选项 1: 通过 Git 仓库部署（推荐）

1. 将代码推送到 GitHub/GitLab/Bitbucket
2. 访问 [vercel.com](https://vercel.com) 并登录
3. 点击 "Add New Project"
4. 导入你的 Git 仓库
5. Vercel 会自动检测配置，直接点击 "Deploy"

### 选项 2: 通过 Vercel CLI 部署

```bash
# 安装 Vercel CLI
npm install -g vercel

# 部署
vercel
```

## 📱 移动端测试

### 在 iOS Safari 测试
1. 确保电脑和手机在同一 WiFi 网络
2. 找到电脑的局域网 IP（例如：192.168.1.100）
3. 运行：`npm run dev -- --host`
4. 在手机浏览器访问：`http://192.168.1.100:5173`

## 🔧 常见问题

### 页面空白？
检查浏览器控制台是否有错误信息

### localStorage 不工作？
- iOS Safari：确保未开启"无痕浏览"模式
- 检查浏览器设置是否禁用了 cookie/localStorage

### 动画卡顿？
- 检查浏览器性能
- 尝试在生产构建中测试：`npm run build && npm run preview`

## 📦 生产构建

```bash
# 构建
npm run build

# 预览生产构建
npm run preview
```

构建产物会生成在 `dist/` 文件夹中。
